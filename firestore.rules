
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isRegisteredUser() {
      return request.auth != null && request.auth.token.firebase.sign_in_provider != 'anonymous';
    }

    function getUserRole(userId) {
      return get(/databases/$(database)/documents/users/$(userId)).data.role;
    }
    
    function isSupervisorOrAdmin() {
        let userRole = getUserRole(request.auth.uid);
        return userRole in ['Site Supervisor', 'Services Manager', 'Corporate Manager', 'Admin', 'Superadmin', 'Data Admin'];
    }
    
    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    // Rules for the users collection
    match /users/{userId} {
      allow read: if isRegisteredUser();
      allow create: if isSupervisorOrAdmin();
      allow update: if isSupervisorOrAdmin();
      allow delete: if isSupervisorOrAdmin();
    }
    
    // Other collections can be read by any registered user
    match /equipment/{equipmentId} {
      allow read: if isRegisteredUser();
      allow write: if isRegisteredUser() && isSupervisorOrAdmin();
    }
    
    match /vsds/{vsdId} {
      allow read: if isRegisteredUser();
      allow write: if isRegisteredUser() && isSupervisorOrAdmin();
    }
    
    match /tasks/{taskId} {
      allow read: if isRegisteredUser();
      // Allow supervisors and admins to create or update tasks
      allow create, update: if isRegisteredUser() && isSupervisorOrAdmin();
    }
    
    match /breakdown_reports/{reportId} {
      allow read: if isRegisteredUser();
      allow write: if isRegisteredUser();
    }
    
    match /completed_schedules/{scheduleId} {
      allow read, write: if isRegisteredUser();
    }
    
    match /messages/{messageId} {
      allow get: if isRegisteredUser() && (request.auth.uid == resource.data.senderId || request.auth.uid in resource.data.recipientIds);
      allow create: if isRegisteredUser() && request.auth.uid == request.resource.data.senderId;
      allow update: if isRegisteredUser() && request.auth.uid in resource.data.recipientIds; // For marking as read
    }

    match /messages/{messageId} {
        allow list: if isRegisteredUser() && (request.query.where.senderId == request.auth.uid || request.auth.uid in request.query.where.recipientIds);
    }

    // Fallback for any other path, deny all
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
