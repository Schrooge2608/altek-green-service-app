
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Insecure. Allows read/write access to all documents.
    // This should be restricted before deploying to production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
