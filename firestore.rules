rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rule to allow any logged-in user to access the database.
    // This is for debugging the diary tracker and save functionality.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
