
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all documents.
    // This is for emergency access and should be secured later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
