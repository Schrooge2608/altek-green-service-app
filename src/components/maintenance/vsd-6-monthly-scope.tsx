'use client';

import { MaintenanceScopeDocument } from '@/components/maintenance-scope-document';

export function Vsd6MonthlyScopeDocument() {
  const title = "VSDs 6-Monthly Service Scope";

  // Using the generic scope document as a placeholder
  return <MaintenanceScopeDocument title={title} />;
}
